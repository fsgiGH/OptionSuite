% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/instruments.R
\name{instrument.theor_price}
\alias{instrument.theor_price}
\title{Theoretical price of an instrument using stored IV}
\usage{
instrument.theor_price(obj, underlying = NULL, ttm = NULL, r = NULL, q = NULL)
}
\arguments{
\item{obj}{An object of class "instrument" (option or underlying)}

\item{underlying}{Current spot price (if NULL, uses stored value)}

\item{ttm}{Time to maturity in years (if NULL, auto-calculated)}

\item{r}{Risk-free rate (if NULL, uses global)}

\item{q}{Dividend yield (if NULL, uses stored value for options, 0 for underlyings)}
}
\value{
Numeric theoretical price (per share)
}
\description{
For options, returns the theoretical price using stored IV.
For underlyings, returns the current price (same as instrument.value).
}
\examples{
call_opt <- make_option(right = "c",
                       strike = 675,
                       expiry = as.Date("2026-06-18"),
                       style = "a",
                       entry_price = 31.23,
                       entry_iv = 0.203,
                       underlying_at_entry = 678.27,
                       dividend_yield_at_entry = 0.011)

instrument.theor_price(call_opt)  # Should match entry price

spy <- make_underlying(symbol = "SPY", entry_price = 450)
instrument.theor_price(spy)  # Returns entry price
}
